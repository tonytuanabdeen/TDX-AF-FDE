Flutter App Scaffold Prompt: FDE BankYou are a senior Flutter developer. Generate a complete, well-structured Flutter project scaffold for a home banking application called "FDE Bank". This is a scaffold only — no real banking logic needed, just clean architecture and placeholder screens. Do not truncate any file. Output every file with its full path.———1. Project & Platform
Flutter project name: fde_bank
Target platform: iOS (include proper ios/ configuration)
Minimum iOS version: 16.0
Use the latest stable Flutter SDK conventions
2. Brand
Primary brand color: #C0001E (FDE Bank red)
Use a ThemeData with ColorScheme.fromSeed(seedColor: Color(0xFFC0001E))
Define the theme in a dedicated lib/theme/app_theme.dart file
App name displayed on device: "FDE Bank"
Use a simple placeholder app icon (red background, white "FB" text — provide flutter_launcher_icons config)
3. Architecture
Use feature-first folder structure:
lib/
  main.dart
  app.dart
  theme/
    app_theme.dart
  core/
    constants/
    services/
    utils/
  features/
    home/
    accounts/
    payments/
    profile/
  shared/
    widgets/
    models/
Use Riverpod for state management (flutter_riverpod)
Use GoRouter for navigation
Use shared_preferences for local persistence (used for the Salesforce Contact ID — see point 5)
4. Screens (placeholder UI, branded)
Generate the following screens, each with an AppBar and a centered placeholder Text widget:
HomeScreen — greeting + placeholder account balance card
AccountsScreen — placeholder list of accounts
PaymentsScreen — placeholder payment form
ProfileScreen — user profile, this is where the Salesforce Contact ID is displayed and editable (see point 5)
Use a BottomNavigationBar (or NavigationBar) with icons for: Home, Accounts, Payments, Profile.5. Salesforce Contact ID Storage
Create a lib/core/services/salesforce_service.dart with:
A method Future<String?> getContactId() — reads from shared_preferences key sf_contact_id
A method Future<void> setContactId(String id) — writes to shared_preferences
A method Future<void> clearContactId() — clears the key

In ProfileScreen, show the stored Contact ID (or "Not linked" if null), with an edit icon that opens a dialog to update it
Expose a salesforceContactIdProvider (Riverpod StateNotifierProvider or AsyncNotifierProvider) so any feature can read it
6. Dependencies (pubspec.yaml)
Include:
flutter_riverpod
go_router
shared_preferences
flutter_launcher_icons (dev dependency)
google_fonts (use a clean sans-serif, e.g. Inter or DM Sans)
7. iOS-specific
Set CFBundleDisplayName to FDE Bank in Info.plist
Set IPHONEOS_DEPLOYMENT_TARGET to 16.0 in Podfile and project.pbxproj
Include a placeholder GoogleService-Info.plist note (comment in README)
8. README
Generate a README.md with:
Project overview
How to run on iOS simulator
How to set/read the Salesforce Contact ID
Architecture overview (short)
"Coming soon" section for future features
Output all files. After the last file, include a summary table: File Path | Purpose.
Place the files under the app/ folder